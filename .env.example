# ──────────────────────────────────────────────────────────────
# Apache Jena Fuseki — OPTIONAL
# Leave empty to use the built-in SPARQL engine (Comunica) over the local ontology.
# Set this only if you want to use a Fuseki server:
#   Local      : http://localhost:3030/webdev/sparql  (run setup-fuseki.bat)
#   Production : https://<fuseki-host>/webdev/sparql
# ──────────────────────────────────────────────────────────────
# FUSEKI_ENDPOINT=http://localhost:3030/webdev/sparql
